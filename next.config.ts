import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['static1.srcdn.com','lh3.googleusercontent.com'],  // Add the external domain here
  },
  /* config options here */
};

export default nextConfig;
